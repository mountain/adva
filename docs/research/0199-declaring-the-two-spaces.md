# 0199 — 先把两个空间声明清楚：桥常数恰为 2，于是两个账可以并在一行

Status: bounded exact experiment, research-only. Evidence:
`experiments/declared_bridge/evidence.json`。本轮执行 0198 结尾建议的第 3 步。

## 1. 桥常数：**恰好 2，而且与深度无关**

| 空间 | 同一深度 d 上的"原子" |
| --- | --- |
| **keraia 族** | 深度 d 的柱，测度 **2⁻ᵈ**；这样的柱有 2ᵈ 个 |
| **wrapper 族** | 切口 d 之后的**尾部**，测度 **2⁻⁽ᵈ⁺¹⁾**（由层质量 `2⁻⁽ⁿ⁺¹⁾` 求和得来，逐层核对） |

两者之比 **尾部 / 原子 = 1/2**，在 **d = 0…20 的每一个深度上都相同** ✓✓ ——
所以：**一个 keraia 单位 = 两个 wrapper 尾部单位**，这是一个**桥**，不是巧合
（我把 21 个深度**逐个**算了，而不是只看深度 15 那一个 ✓）。

## 2. 于是 0198 那个 13378 分解清楚了

> **6689 个未解出原子，每个重 1 个原子，用"半个原子"作单位来称，就重两倍** → **6689 × 2 = 13378** ✓

**没有神秘之处**了 ✓。

## 3. 端到端**第一行同时带两种货币的账**（本轮的交付物）

| 项 | 值 |
| --- | --- |
| 深度 | **15** |
| 声明的对应 | **一层协议 = 一个深度单位**（**声明，非测量**） |
| 桥常数 | 尾部 = 半个原子 ⇒ **一个 keraia 单位 = 两个 wrapper 单位**，逐深度相同 |
| **质量账** | 已接受 **26078/32768**（508 原子）· 已证不停机 **1/32768** · 待输入 **254/32768** · 语法不全 **6435/32768** |
| 未解出 | **6689/32768 ≈ 20.41%** |
| **成本账** | 准备金 **126 步** · 侧边 **256 步** · 比值 **63/128** |

## 4. 这一行**不**说的话（被拒绝的读法）

> ~~"走得层数越多，保留族里被解出的就越多"~~ ✗

**层数决定的是"那个深度能否到达"**，而**未解出的原子数是族的性质**，与遍历无关 ✓✓。
协议解出的是**它自己的空间**（那里的尾部逐层减半）；族的未解出原子是**另一个量**，
桥常数负责**换算单位**，而**不把两者合并** ✓。

## 5. 两条声明状态（必须跟着这一行走）

1. **一层 ↔ 一个深度单位**：**声明，不是测量** ✗（检查器把这句话写进证据 ✓）；
2. **只有深度 15 有保留分区** → 这一行**只存在于那里**，别处**没有质量账可给** ✓（也逐项核对 ✓）。

## 6. 本轮记录在案的错误

本轮有两处我自己写错，**都留在记录里**：

1. **分解公式写错**：写成 `(尾部/原子)⁻¹ × 2`，正确的是 **`6689 × (原子/尾部)`** ✗ —— 被检查当场拦下；
2. **把"深度数"读成"最大深度"**：契约写 `declared_depths: 21`（**数量**），代码当成"到深度 21"，
   于是算出了 **22 行**而不是 21 行 ✗ —— 被测试拦下。**"数量"与"上界"是两个量**，
   这正是本仓反复记的那类错。

本轮仍是 **`exact-only`**（无浮点、无墙钟）。

## 7. 本轮**没有**主张的

- 这一行是**在某条声明之下的比较**，**不是**被发现的同一性 ✗；
- **没有**在两个空间之间搬运质量：桥只**换算单位** ✓；
- 成本列经过**声明的换算**（8 层步/实测单位），密钥一半**仍未测**；
- **没有**计算 Ω 的值、**没有**主张可计算性；**三侧张力**照旧未化解（0192 §8）。
