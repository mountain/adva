# 0156 评审决策草案（assistant 起草，供 Mingli Yuan 评审）

日期：2026-09-07。状态：`proposed-document`，建议草案，不改变任何既有登记；
全部条目仅待批准/修改/否决。对应 0156 第 12 节八问与 merge 排演暴露的两个
设计点。

## 八问建议

| # | 建议答案 | 理由 | 解锁 |
| --- | --- | --- | --- |
| 1 | 划去原问（Phase 0、1 均已实现），替换为"Phase 2 是否启动" | 清单与现实同步 | 决策清单更新 |
| 2 | **STARK（透明设置）** | 无仪式与毒废料；贴合"拒绝未认证来源"；证明大但研究场景可接受 | Phase 2 路线 |
| 3 | git 签名 tag + 三包互嵌作为 v0 锚点分发；外部透明度日志延后 | 现有渠道足以守住"全部副本被攻破"边界；日志属运维决策 | 锚点纪律固化 |
| 4 | **IVC 永久搁置**；O(n) 重放为唯一路径 | 10^5 记录秒级可验；IVC 引入额外密码假设与证明成本 | 简化 Phase 3 |
| 5 | 跨包引用格式 v0 预留三包；启用仅 arithmetic | 未来分离成本低；当前证据仅在算术 | 三包分离预案 |
| 6 | 登记进 `math/manifest.json`，home=arithmetic | 0156 主体是算术谱系工具；登记按 0154 纪律（documentary，不授权威） | 目录完整性 |
| 7 | 密钥保管者暂定"项目所有者离线保管 + 用途域分离"；Python 锁定 `cryptography`（已实测）；Rust 锁定 `ed25519-dalek`（仓库既有依赖） | 与 0155 第 3 节已列接口一致 | 生产级 key-issue |
| 8 | 向量按 RFC 8032 §7 / RFC 8439 附录 pin 进 tests；分歧仲裁=停止准入、保留差异；记录者=项目所有者 | 沿用 0155 §8.1 既有纪律 | 验证可信度 |

## merge 两个设计点建议

- `order_score`/`order_rank`：升级 record schema **v1**（新增可选非负整数字段），
  替代现记入 environment 的过渡方案；建议在 Phase 2 之前完成；
- `merge-signing` 用途域：key-issue 的 PURPOSES 增加 `merge-signing`
  （当前仅有 `anchor-signing`）。

## 0157 谓词

不变，等待独立评审（批准/修改/否决）。

## 边界

本草案全部为建议；不批准不实施；不修改任何已冻结合同与已发布证据。
