# Research 0161 evidence

Byte copies of the local AEG trials (repo `AEG`, commits `f7de663`,
`21d7196`, `275ee5d`). Two sub-trials:

1. advance receipts ([design](design-advance-receipt.md)): the five-state stutter classification of 0163's next
   minimum step (EvidenceStutter / VariationObserved / IncidentalVariation /
   Rejected / Unknown) with receipts 01-03.
2. iota/SKI substrate projection ([design](design-iota-projection.md)) of the 0164 byte formula: signed sign-pair
   church arithmetic over the full 0..255 byte set, 256/256 values correct,
   256/256 distinct terms, tamper controls and iota round-trips pass.

Registered as local research note 0161 (fills the remote numbering gap);
no claim entry in claims.toml, no native admission, no new epoch.
