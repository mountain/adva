# Publication correction — 2026-09-16

Mingli Yuan requested urgent correction across adva, adva-library and adva-machine.
A directory import carried third-party full texts and rendered document pages
into public repositories, although receipts 21, 24, 25 and 26 limited these
materials to local research. Public redistribution permission has not been
established. This correction withdraws the identified copies; it does not make a
legal infringement finding or withdraw the original research, attribution,
measurements, failures, or historical receipt statements.

The exact file identities and sizes are recorded in
`governance/withdrawals/2026-09-16-third-party-documents.json`.
Source documents can be identified through their original research notes and
publisher/arXiv records. A digest identifies bytes; it does not provide access,
prove permission, or make the historical experiment independently reproducible
without lawfully obtained inputs. Historical local-only receipts remain unchanged.

This first correction removes copies from the current tree. Historical objects,
other refs, GitHub cached views, PR refs and third-party clones require separate
verification; this notice does not claim those copies have been erased.
Do not merge pre-correction history or restore the withdrawn files when syncing.
The corresponding history-remediation record will state its actual scope.

Original human and AI contributions remain attributed. This correction was
prepared by ChatGPT (OpenAI), through Mingli Yuan's authorized GitHub account
proxy; account use is not personal authorship, review or a correctness guarantee.

## Execution and historical replay boundary

The publication-clean rehearsal rebuilt the pinned machine from source. The
library arithmetic observer and three data-machine outcomes (Returned 14,
Rejected unsupported tag, and Suspended at quantum 17) passed fresh native
verification. This is finite continuity evidence, not a claim that every
historical experiment can be replayed without its external inputs.

The golden-ratio calibration still rejects incomplete input sets. Its public
regression suite checks the eight declared withdrawals and explicit refusal;
four byte-complete replay/tampering checks require lawfully obtained private
inputs and are reported as skipped when those inputs are absent. The frozen
index, contracts and earlier Passed receipt remain byte-for-byte historical.

History correction changes commit IDs. The checked map under
`governance/withdrawals/history-map-2026-09-16.json` records replacements while
preserving author/committer identity and dates, messages, parent topology and
all nonwithdrawn file bytes. The frozen-library CI resolves the recorded commit
before applying the same strict source-byte and fingerprint checks. Old
research contracts that bind a whole removed resource set or its Git tree may
still refuse; the map does not waive their contracts or authorize a new trial.

After correction, use a fresh clone. Do not merge a pre-correction branch or
run acquisition commands copied from historical receipts without consulting
the correction map and the active dependency lock.

## Completed writable-history correction

After Mingli explicitly released the force-push prohibition for this correction,
[the atomic history job](https://github.com/mountain/adva/actions/runs/35098998296)
completed. A fresh clone confirmed all 184 writable branch refs against the
published mapping and found zero reachable identified withdrawn blob objects.
The 1,146 changed commits were checked for original author/committer identity
and dates, message bytes, parent topology, and every unaffected file byte/mode.
The packed fresh-clone history measured 49.99 MiB; GitHub's repository-size
counter and retained PR objects may differ.

The fixed clean machine/library dependency remains unchanged. Complete Rust and
Python 3.11/3.12/3.13 matrices passed on the withdrawal implementation before
history publication; the final clone separately rechecks mapped source
references, dependency/receipt boundaries, and known-content history. Four
full-input golden-ratio checks remain explicitly unavailable without lawful
external inputs. The optional ed25519 test also skips when its backend is absent.

The one-time publishing workflow is removed after completion. The ongoing
publication check has read-only permission and scans reachable branch history.
Ruleset 22887616 must have **Block force pushes** restored after this maintenance
window; its deletion protection remained active throughout. Existing PR refs,
cached old objects and third-party clones remain outside this result.
